<resources>
    <!-- Base application theme. System bar appearance, colors, and contrast are configured at
         runtime via enableEdgeToEdge() in MainActivity. -->
    <style name="Base.Theme.PolarRecorder" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.PolarRecorder" parent="Base.Theme.PolarRecorder" />
</resources>